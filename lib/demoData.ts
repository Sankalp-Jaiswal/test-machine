import { TestBank } from "@/types";

// Demo seed removed — the dashboard now reflects the user's actual question
// pool, with a clear empty state when no imports exist yet.
export const DEMO_TESTS: TestBank[] = [];
